namespace Solution1.ConsoleApp1
{
    using System;
    using System.Diagnostics;
    using System.Collections.Generic;
    using System.Linq;
    using ClassLibrary1;

    public class Program1
    {
        /*public static void Main(string[] args)
        {
            IDocotorRepository personRepository = new PersonRepository() as IDocotorRepository;

            Debug.Assert(condition: personRepository != null);

            foreach (Person person in personRepository.Find(PersonsSecondName.Andrzej))
            {
                string personDescription = person.GetDescription();
                string personAdditionalDescription = person.GetAdditionalDescription();

                Console.WriteLine("Osoba:\n opis podstawowy = {0},\n opis dodatkowy= {1}\n" , personDescription, personAdditionalDescription);
                
            }
            Console.ReadLine();*/
        }
    }
}
